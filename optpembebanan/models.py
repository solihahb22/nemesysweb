from django.db import models
from setparam.models import UnitBoiler, ParameterOptPembebanan

# Create your models here.
"""
class OptimasiBebanUnit(models.Model):
    unit = models.OneToOneField(
        UnitBoiler,
        on_delete=models.CASCADE,
        primary_key=True
    )
    cost = models.DecimalField(max_digits=9,decimal_places=0)
    nphr = models.DecimalField(max_digits=9,decimal_places=4)
    biayaproduksi = models.DecimalField(max_digits=9,decimal_places=0)
    rekomendasikalor = models.IntegerField(max_length=)

"""




